Django==5.1.1
waitress==3.0.0
# numpy 2.x 的官方 wheel 要求 CPU 支持 x86-64-v2（popcnt 等指令）。
# 部署目标 mesdevserver 是 Core2 Duo，没有 popcnt，装 2.x 会在 import 时
# 直接 RuntimeError: NumPy was built with baseline optimizations (X86_V2)。
# 1.26.4 是最后一个不强制 v2 的版本，必须锁住。
numpy==1.26.4
pandas==2.2.3
xlrd==2.0.1
openpyxl==3.1.5
whitenoise==6.7.0
