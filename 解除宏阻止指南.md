# Excel 宏阻止问题 - 完整解决方案

## 方法一：解除单个文件阻止（推荐给最终用户）

1. **右键点击** `saledemoA.xls` 文件
2. 选择 **属性**
3. 在"常规"选项卡底部，勾选 **"解除锁定"** (Unblock)
4. 点击 **确定**
5. 重新打开文件

## 方法二：通过 PowerShell 批量解除（适合管理员）

```powershell
# 解除单个文件
Unblock-File -Path "E:\DEV\dailyReport\outputs\saledemo\saledemoA.xls"

# 解除整个目录
Get-ChildItem "E:\DEV\dailyReport\outputs" -Recurse | Unblock-File
```

## 方法三：降低 Excel 宏安全级别（不推荐，有风险）

1. 打开 Excel
2. **文件 → 选项 → 信任中心 → 信任中心设置**
3. **宏设置** → 选择"启用所有宏"
4. ⚠️ **风险**：此设置会让所有宏文件都能运行，包括恶意宏

## 方法四：添加受信任位置（推荐给企业部署）

1. 打开 Excel
2. **文件 → 选项 → 信任中心 → 信任中心设置**
3. **受信任位置 → 添加新位置**
4. 浏览到 `E:\DEV\dailyReport\outputs`
5. 勾选 **"同时信任此位置的子文件夹"**
6. 确定

**优点**：只信任指定目录，其他位置的宏仍会被阻止，安全性较高。

---

## 当前文件状态检查

运行以下脚本可以检查文件是否被标记为"阻止"：

```powershell
Get-Item "E:\DEV\dailyReport\outputs\saledemo\saledemoA.xls" -Stream Zone.Identifier -ErrorAction SilentlyContinue
```

- 如果有输出 → 文件被标记，需要解除
- 如果报错"找不到"→ 文件没有被标记
